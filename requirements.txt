# Core dependencies
httpx>=0.24.0
pydantic>=2.0.0

# Unibase AIP SDK (for agent registration and platform communication)
-e /home/ubuntu/unibase-aip/aip/sdk

# AI SDKs (optional - install as needed)
anthropic>=0.18.0
openai>=1.0.0
langchain>=0.1.0
langchain-anthropic>=0.1.0

# Memory middlewares (optional - install as needed)
# mem0ai>=0.1.0
# zep-python>=1.0.0
# pymemgpt>=0.2.0

# Web3 (optional)
# web3>=6.0.0
# eth-account>=0.9.0

# Cryptography (optional - for Ed25519 signing)
cryptography>=41.0.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
