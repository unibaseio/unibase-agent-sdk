# Core dependencies for unibase-agent-sdk
# This file mirrors pyproject.toml dependencies for pip install -r compatibility

httpx>=0.24.0
pydantic>=2.0.0

# Unibase AIP SDK (install from local package or PyPI)
# pip install -e ../unibase-aip-sdk
# or: pip install unibase-aip-sdk
